spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_mgr
    username: root
    password: IshaaN310#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
